<blockquote class="blockquote">
    <slot />
</blockquote>

<style>
    .blockquote {
        margin: 1rem;
        background-color: color-mix(
            in srgb,
            var(--accent-color) 10%,
            var(--main-color)
        );
        border: 1px solid var(--accent-color);
    }
</style>
